# frozen_string_literal: true

require "jekyll"
require_relative "../lib/jekyll_random_hex"

RSpec.describe(RandomNumberTag) do
  it "is created properly" do
    # random_number_tag = RandomNumberTag.new("random", "3", nil)
    # n = random_number_tag.render(nil)
    # expect(n.length).to eq(6)
  end
end
